<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Meteo-&#9728;</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <div class="container">
      <fieldset>
        <form action="/" method="post">
          <input name="città" type="text" class="ghost-input" placeholder="Inserisci una città" required>
          <input type="submit" class="ghost-button" value="Quanto caldo fa?">
        </form>
        <% if(weather !== null){ %>
          <p><%= weather %></p>
        <% } %>

        <% if(error !== null){ %>
          <p><%= error %></p>
        <% } %>
      </fieldset>
    </div>
  </body>
</html>
